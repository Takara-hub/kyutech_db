import PrimaryButton from "../../components/PrimaryButton";

const Login = () => {
  return (
    <div>
      Hello World!
      <PrimaryButton></PrimaryButton>
    </div>
  );
};

export default Login;
